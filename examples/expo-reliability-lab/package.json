{
  "name": "@florextech/expo-reliability-lab",
  "private": true,
  "main": "expo/AppEntry",
  "scripts": {
    "start": "pnpm --filter @florextech/core build && pnpm --filter @florextech/storage-sqlite build && pnpm --filter @florextech/storage-expo build && expo start --clear",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.mjs",
    "coverage": "c8 --all --include 'src/**/*.ts' --reporter lcov --reporter text --reporter json-summary node --test test/*.test.mjs"
  },
  "dependencies": {
    "@florextech/core": "workspace:*",
    "@florextech/storage-expo": "workspace:*",
    "expo": "~57.0.23",
    "expo-sqlite": "~57.0.3",
    "react": "19.2.0",
    "react-native": "0.83.2"
  },
  "devDependencies": { "@types/react": "^19.2.0", "c8": "^12.0.0", "typescript": "~5.9.3" }
}
